import 'package:flutter/material.dart';

BoxShadow simpleBoxShadow =  BoxShadow(
                    color: Colors.grey.withOpacity(0.3),
                    blurRadius: 4,
                    offset: const Offset(2, 6),
                    );
